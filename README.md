# DIN HMCC

Repository Link: ([CRUD_App_ADT_DIN](https://github.com/SchoolGroup3/CRUD_App_ADT_DIN))

## Database Setup

1. In the NetBeans project directory (`CRUD_App_ADT_DIN`), locate the file **`CRUD_ADT.sql`** in the SQL folder.
2. Open this file in **MySQL Workbench** and run it. This will create the database and insert the initial data.
3. After that, configure the **`configClase.properties`** file, which can be found in the same directory.
